# Python cache and build artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Git
.git/
.gitignore

# IDE and editor files
.vscode/
.idea/
.cursor/
*.swp
*.swo
.DS_Store

# Documentation (not needed at runtime)
*.md

# Data and outputs (not needed in image)
experiments/
runs/
logs/
*.png
*.jpg
noise_exploration.png
comparison_plots/

# Configuration files (use env vars or ConfigMaps instead)
*.json
*.jsonl
mcp_config.json
gpus.csv

# Kubernetes and infrastructure (managed externally)
*.yaml
*.yaml.bak
k8s-build-job.yaml
pod-monitor.yaml
ray-job*.yaml
nvidia-device-plugin*.yaml
kuberay/

# Terraform (infrastructure as code - not needed in runtime)
tf/
terraform.tfstate
terraform.tfstate.backup

# Development and testing scripts (not needed at runtime)
plot.py
run_test.sh
test*.py
generate_dataset.py
prepare_upload.py
update_dashboard.py

# Grafana dashboards (deploy separately)
*dashboard*.json

# Docker itself
.dockerignore
Dockerfile.dev
Dockerfile.backup

# Keep Dockerfile for reference but it's not copied anyway
Dockerfile
